{{>layouts/header}}

<table class="table">
    <tbody>
        <tr>
            <th scope="row">{{article.id}}</th>
            <td>{{article.title}}</td>
            <td>{{article.content}}</td>
        </tr>
  </tbody>
</table>

{{>layouts/footer}}